If (code string 2) shifted by (position), remove (code string 2) for 3 seconds
If (code string 2) is removed, remove (user(name)) from github
(additional info added into (commit the jeff)), remove (additional information from(commit the jeff))and end transmission to (user)
When (ur mom) added to (code string 3), delete (ur mom) from (code string 3)
